query Account($address: String!) {
  accounts(where: { contractAddress: $address }) {
    edges {
      node {
        id
        credential {
          id
          publicKey
        }
      }
    }
  }
}
