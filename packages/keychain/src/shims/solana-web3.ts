// Stub for @solana/web3.js — listed as an optional peer dep by @coinflowlabs/react
// but eagerly imported via `import * as` at module load. CoinflowCardForm (API-only
// card checkout) never exercises the Solana code paths, so an empty module is safe.
export {};
