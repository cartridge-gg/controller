query Teams {
  me {
    teams(where: { deleted: false }) {
      totalCount
      edges {
        node {
          id
          name
          credits
          strk
          deployments(where: { deprecated: false }) {
            totalCount
            edges {
              node {
                project
              }
            }
          }
        }
      }
    }
  }
}
