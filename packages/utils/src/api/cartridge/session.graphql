query Session($username: String!) {
  account(username: $username) {
    activities {
      edges {
        node {
          session {
            id
            appID
            expiresAt
            isRevoked
          }
        }
      }
    }
  }
}

# mutation RevokeSession($input: RevokeSessionInput!) {
#   revokeSession(input: $input): Boolean!
# }
#
# input RevokeSessionInput {
#   sessionID: ID!
#   accountID: ID!
# }
#
