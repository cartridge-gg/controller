query Session($username: String!) {
  account(username: $username) {
    id
    activities {
      edges {
        node {
          id
          controllerID
          status
          type
          network
          
        }
      }
    }
  }
}
