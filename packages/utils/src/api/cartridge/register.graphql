mutation Register(
  $chainId: String!
  $owner: SignerInput!
  $session: SessionInput!
  $username: String!
) {
  register(
    chainId: $chainId
    owner: $owner
    session: $session
    username: $username
  ) {
    name
    username
  }
}
