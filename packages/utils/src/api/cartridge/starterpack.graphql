query StarterPack($id: ID!) {
  starterpack(id: $id) {
    name
    description
    active
    price
    bonusCredits
    issuance
    maxIssuance
    starterpackContract {
      edges {
        node {
          name
          description
          iconURL
        }
      }
    }
  }
}
